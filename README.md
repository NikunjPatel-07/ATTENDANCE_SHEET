# ATTENDANCE_SHEET
🚀 Features  Reads student records from ATTENDANCE SHEET.txt  Accepts multiple absent roll numbers from the user  Searches and matches roll numbers with names  Displays a clean formatted list of absent students  Handles invalid or missing roll numbers gracefully  Supports Windows &amp; Linux screen-clear commands
